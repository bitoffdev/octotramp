// Number of pixels traveled on the x-axis per jump
export const JUMP_DISTANCE = 500;
// The maximum speed at which to scroll (pixels per frame)
export const MAX_SCROLL_VELOCITY = 13;
export const TRAMPOLINE_WIDTH = 100;
export const TRAMPOLINE_HEIGHT = 40;
export const CHARACTER_WIDTH = 80;
export const CHARACTER_HEIGHT = 80;

export const GAME_HEIGHT = window.innerHeight;
export const GAME_WIDTH = window.innerWidth;
